import React from 'react'
import ReactDOM from 'react-dom/client'
import VibeApp from './VibeApp.jsx'
import '../index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <VibeApp />
  </React.StrictMode>,
)
