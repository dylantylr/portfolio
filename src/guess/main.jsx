import React from 'react'
import ReactDOM from 'react-dom/client'
import GuessApp from './GuessApp.jsx'
import '../index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <GuessApp />
  </React.StrictMode>,
)
